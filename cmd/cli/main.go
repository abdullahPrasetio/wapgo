// Package main is the entrypoint for the wapgo CLI.
// The CLI provides project scaffolding (wapgo new) and code generators
// (wapgo make:*). Full implementation is in Fase v0.4 of devplan.md.
package main

import "fmt"

func main() {
	fmt.Println("wapgo CLI — coming in v0.4")
}
